# Frontend

Local React/Vite frontend for the audit workflow.

## Run

1. Install dependencies:
   `cd frontend`
   `npm install`
2. Start the frontend:
   `npm run dev`
3. The app runs on:
   `http://localhost:5173`

The Vite dev server proxies `/api/*` to the Flask API server on `http://localhost:7002`.
